[tools]
node = "20"
npm = "latest"

[env]
NODE_ENV = "development"
ASPNETCORE_ENVIRONMENT = "Development"

[tasks.dev]
description = "Run all services concurrently (bot, API, and frontend)"
depends = ["//:dev"]
